import React from 'react';

const GlobalConfigViewer: React.FC = () => {
  // Placeholder: later we'll load the Anchor IDL and program account
  return (
    <div>
      <h2>Global Config</h2>
      <p>Program ID and GlobalConfig will be loaded here.</p>
    </div>
  );
};

export default GlobalConfigViewer;
